---
phase: 11-iterative-synthetic-discovery-loop-v1
plan: "01"
wave: 1
depends_on: []
autonomous: true
requirements: [LOOP-01]
files_created:
  - .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
files_modified: []
touches_production_code: false
may_incur_openai_cost: false
verification_commands:
  - "test -f .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md"
  - "grep -q 'Authoritative' .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md"
  - "grep -q 'failure_backlog.json' .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md"
  - "grep -q 'regression_test_needed' .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md"
  - "git diff --name-only src/basic_stats/ | wc -l  # must return 0"
commit_expectations:
  - .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
---

# Plan 11-01: Phase 11 SPEC

## Objective

Author `11-SPEC.md` as the **single authoritative contract** for Plans 11-02 through 11-07. Lock the artefact layout, the failure-memory backlog schema, the 7-action-type triage rubric enum, the campaign-generator contract, the iteration-runner compare-runs semantics, the promotion rules, the verification campaign contract, the exit gates, and the non-goals. Mirrors how Plan 10-01 wrote `10-SPEC.md` for Phase 10.

## Files Likely Created / Modified

| Path | Operation |
|------|-----------|
| `.planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md` | Create or verify-and-lock |

No other files touched. `src/basic_stats/*` zero diff.

## Read First (canonical refs)

- `.planning/phases/11-iterative-synthetic-discovery-loop-v1/11-CONTEXT.md` — design decisions and locked schemas
- `.planning/phases/10-self-generated-robustness-synthetic-uat/10-SPEC.md` — inherited contracts (taxonomy, fixture schema, runner contract, cluster schema, regression workflow)
- `.planning/phases/10-self-generated-robustness-synthetic-uat/10-06-SUMMARY.md` — DuckDB `--workers 1` constraint and live-run evidence
- `evals/synthetic_clusterer.py` — `_signature()` and `cluster_failures()` — backlog entries derive from these
- `evals/synthetic_runner.py` — runner contract that the iteration runner wraps
- `evals/synthetic/seed_questions.json` — fixture schema that campaign generator output must conform to

## Tasks

### Task 1 — Author / verify 11-SPEC.md

<read_first>
- .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-CONTEXT.md
- .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md (if it exists from planning — verify and tighten, do NOT re-write from scratch)
- .planning/phases/10-self-generated-robustness-synthetic-uat/10-SPEC.md
- evals/synthetic_clusterer.py
</read_first>

<action>
A draft `11-SPEC.md` was authored during planning. Verify the file exists and contains the following 13 numbered sections, in order:

1. Goal — one paragraph, names the loop steps in order
2. Artefact Layout (locked) — tree of `evals/discovery/`
3. Failure-Memory Backlog Schema (locked) — exact JSON shape including `id`, `first_seen_run_id`, `last_seen_run_id`, `seen_count`, `category`, `failure_signature`, `guard_evidence`, `question_ids`, `sample_answers`, `diagnosis`, `status`, `recommended_action`, `promoted_to`, `linked_campaign`, `notes`
4. Triage Rubric Action Types (locked enum) — 7 types: `regression_test_needed`, `agent_behavior_fix_needed`, `context_missing`, `helper_tool_needed`, `fixture_issue`, `non_actionable`, `deferred`
5. Backlog Status Lifecycle — state diagram (open → triaged → promoted → resolved; alternates: deferred, non_actionable)
6. Targeted Campaign Generator Contract — public API + output schema + template/LLM modes
7. Iteration Runner / Compare-Runs Semantics (locked) — public API + `repeated`/`disappeared`/`mutated`/`new` definitions + diff JSON shape
8. Promotion Rules (locked) — eligibility (`seen_count >= 2`), per-action emission table, hard rules
9. Verification Campaign Contract (Wave 7) — seeded from `unsupported_future__refuse_expected_but_answered`
10. Exit Gates — 9 gates, satisfied-by column references plans 11-01..11-07
11. Non-Goals — explicit out-of-scope list including production code changes
12. Commit Hygiene Reminders — explicit paths only, gitignore checks
13. Inheritance from Phase 10 — names `10-SPEC.md` sections that this SPEC reuses verbatim

If any section is missing or contradicts `11-CONTEXT.md`, edit the file in place. Do NOT rewrite from scratch unless the existing draft is fundamentally inconsistent with `11-CONTEXT.md`.
</action>

<acceptance_criteria>
- File exists: `test -f .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md`
- File contains all 7 action-type enum values: `grep -c 'regression_test_needed\|agent_behavior_fix_needed\|context_missing\|helper_tool_needed\|fixture_issue\|non_actionable\|deferred' 11-SPEC.md` returns at least 7
- File contains the backlog schema: `grep -q 'backlog_version' 11-SPEC.md` AND `grep -q 'failure_signature' 11-SPEC.md`
- File contains compare-runs buckets: `grep -E 'repeated|disappeared|mutated|new' 11-SPEC.md` returns at least 4 hits
- File contains the verification seed cluster name: `grep -q 'unsupported_future__refuse_expected_but_answered' 11-SPEC.md`
- File contains the `--workers 1` inheritance reference: `grep -q 'workers 1' 11-SPEC.md`
- File contains the read-only constraint reference: `grep -q 'src/basic_stats' 11-SPEC.md`
- `git diff --name-only src/basic_stats/` returns empty
</acceptance_criteria>

### Task 2 — Cross-check inheritance from Phase 10

<read_first>
- .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
- .planning/phases/10-self-generated-robustness-synthetic-uat/10-SPEC.md
</read_first>

<action>
Confirm the SPEC's "Inheritance from Phase 10" section names every reused section by number: §2 (taxonomy), §3 (question schema), §4 (runner contract), §5 (per-question result schema), §6 (cluster schema), §7 (regression workflow). If any of these references is missing, add it.

Confirm the SPEC does NOT redefine the 12-category taxonomy or the question-fixture schema — these MUST be inherited by reference, not duplicated. If duplicates exist, replace with a one-line reference to `10-SPEC.md §<N>`.
</action>

<acceptance_criteria>
- `grep -c '10-SPEC.md' 11-SPEC.md` returns at least 4 (referenced from at least 4 places)
- The 12 taxonomy enum values (DIRECT_STATS, RANKINGS, COMPARISONS, FOLLOW_UPS, TOP6_VS_BIG6, CHAMPIONS_LEAGUE, P90_METRICS, HOME_AWAY, SPANISH_ENGLISH, UNSUPPORTED_FUTURE, AMBIGUOUS, DEMO_RANDOM) are NOT redeclared as a code block in `11-SPEC.md` — `grep -c 'DIRECT_STATS' 11-SPEC.md` returns 0 OR exactly 1 (acceptable inline reference) but NOT a block listing all 12 (which would be a duplication of `10-SPEC.md §2`)
</acceptance_criteria>

## Verification

```bash
test -f .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
grep -q 'Authoritative' .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
grep -q 'failure_backlog.json' .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
grep -q 'regression_test_needed' .planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md
test "$(git diff --name-only src/basic_stats/ | wc -l)" -eq 0
```

## Commit Expectations

Stage and commit only:
- `.planning/phases/11-iterative-synthetic-discovery-loop-v1/11-SPEC.md`

Suggested commit message: `docs(11): author Phase 11 SPEC for iterative discovery loop v1`

NEVER stage: `evals/runs/`, `db/basic_stats.duckdb`, `.claude/settings.local.json`, `.planning/config.json`, `docs/review/*`.

## Must-Haves (goal-backward)

- Every later plan (11-02..11-07) references `11-SPEC.md` in its `<read_first>` block.
- The SPEC is **authoritative**: any ambiguity discovered by later plans is resolved by editing `11-SPEC.md`, not by the consuming plan inventing new contracts.
- The SPEC defers to `10-SPEC.md` for inherited contracts — Phase 11 does not fork those contracts.
